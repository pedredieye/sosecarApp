{% extends 'templates/layout.twig' %}
{% block page_title %}Supprimer{% endblock %}
{% block active_participant %}active{% endblock %}
{% block collapse_in %}in{% endblock %}
{% block page_title_breadcrumb %}Suppression de participants{% endblock %}
{% block content %}


<section class="forms-validation">
  <div class="page-header">
    <h1>      <i class="md md-group"></i>     Supprimer une participation   </h1>
    <p class="lead d-none">The <a href="http://1000hz.github.io/bootstrap-validator/" target="_blank">Validator plugin</a> offers automatic form validation configurable via mostly HTML5 standard attributes. It also provides an unobtrusive user experience, because nobody likes a naggy form.</p>
  </div>
  <div class="row  m-b-40">
    <div class="col-md-3 col-md-push-9 ">
      <h5 class="d-none">Aide</h5>
      <p class="d-none"> Push the submit button to check all the validations at once. Resetting will handle resetting the validation messages. </p>
    </div>
    <div class="col-md-8 col-md-pull-3">

      <div class="bs-component" >
        <div class="alert alert-dismissible alert-success d-none" id="alert-form">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <h4>Success!</h4> <strong>Well done!</strong> You successfully read <a href="#" class="alert-link">this important alert message</a>.
        </div>
      </div>

      <div class="card bordered">
        <div class="card-header alert alert-danger"> <span class="card-title">Êtes-vous sûr(e) de vouloir supprimer ?</span> </div>
        <div class="card-content">
          <p>Vous êtes sur le point de supprimer la participation de {{data.participant.first_name}}  {{data.participant.last_name}}. </p>
          <p>Titre :   {{data.participant.title.title}}</p>
          <p>Fonction :   {{data.participant.job.job_title}}</p>
          <p>Pays :   {{data.participant.country.name}}</p>
        </div>
        <div class="card-action clearfix">
          <button href="#" class="btn btn-primary ml-2" id="btn-delete-part" data-participant-ticket = "{{data.participant.ticket_number}}" data-participant-id = "{{data.participant.id}}"  >Valider la suppression</button>
          <button href="#" class="btn btn-default">Annuler</button>
        </div>
      </div>


    </div>
  </div>
</section>


{% endblock %}
